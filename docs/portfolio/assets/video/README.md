# Backup Demo Recording Status

No recording is committed or published yet. This directory intentionally contains only the recording contract so a missing video cannot be mistaken for a finished deliverable.

Use the 2–3 minute shot list and privacy checklist in the [Demo Script](../../DEMO_SCRIPT.md). After a real recording is reviewed:

1. publish it to the approved portfolio host or add the appropriately sized versioned file;
2. record its public HTTPS URL, duration, capture date and SHA-256 here;
3. confirm it contains no environment variables, authorization headers, real email addresses, passwords, browser autofill, provider dashboards or genuine customer inputs;
4. play it once offline alongside the static Eval report to verify the fallback narrative.

Status: **blocked on human recording and privacy review**.
